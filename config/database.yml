# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
default: &default
  adapter: postgresql
  encoding: unicode
  pool: 5
  timeout: 5000
development:
    «: *default
    database: bosti_development
    test:
      «: *default
      database: bosti_test
      staging:
        url: <%= ENV['DATABASE_URL'] %>
        production:
          url: <%= ENV['DATABASE_URL'] %>
